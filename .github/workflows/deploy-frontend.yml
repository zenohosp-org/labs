name: Deploy frontend to VPS
on:
  push:
    branches: [main]
    paths:
      - 'labs-frontend/**'
# Both deploy workflows git-pull the same VPS checkout (/opt/zenohosp/labs);
# a shared group serializes them so concurrent pulls can't race on the ref.
concurrency:
  group: vps-deploy
  cancel-in-progress: false
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Build and deploy over SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          command_timeout: 20m
          script: |
            set -e
            cd /opt/zenohosp/labs
            git pull origin main
            cd labs-frontend
            npm ci
            npm run build
            rm -rf /var/www/labs/*
            cp -r dist/* /var/www/labs/
